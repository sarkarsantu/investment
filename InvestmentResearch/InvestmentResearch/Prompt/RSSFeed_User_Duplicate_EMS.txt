Follow these rules precisely:

1. SEMANTIC DEDUPLICATION & RANKING LOGIC:
   - Match by Meaning: Analyze the "Title" and "Content" fields for conceptual equivalence. If two entries describe the exact same news event (e.g., "PM Modi inaugurates Sanand plant" vs "New chip plant launched in Gujarat by Prime Minister"), classify them as duplicate items.
   - Group Identical Information: Group all items sharing the same core meaning together.
   - Assign Ranks: 
     - Generate a global counter starting at 1 for each distinct news group.
     - Assign this identical counter integer to the "Rank" field of ALL items belonging to that same semantic group. 
     - Do not increment the rank number when encountering an entry that belongs to a group you have already numbered.
   - Mark Flag: For any item that shares its core meaning with another entry in the dataset, change its "IsDuplicate" field to true. If an item covers a completely unique piece of news found nowhere else in the array, leave "IsDuplicate" as false.

2. OUTPUT FORMAT RULES:
   - Output ONLY the final, modified valid JSON array matching the exact incoming schema structure.
   - CRITICAL: Do not wrap the output in markdown code fences like ```json or ```. Start the output directly with the character [ and end it directly with ].
   - Do not append or prepend any conversational padding, greetings, markdown backticks, or post-analysis notes.

3. INPUT DATA:
[Insert Your JSON Here]